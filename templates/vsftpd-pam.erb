auth       sufficient      pam_userdb.so db=<%= scope.lookupvar('vsftpd::main_config_dir') %>/<%= scope.lookupvar('vsftpd::vsftpd_db_file') %>
account    sufficient      pam_userdb.so db=<%= scope.lookupvar('vsftpd::main_config_dir') %>/<%= scope.lookupvar('vsftpd_db_file') %>

auth       sufficient      pam_pwdfile.so pwdfile <%= scope.lookupvar('vsftpd::main_config_dir') %>/<%= scope.lookupvar('vsftpd_pwd_file') %>
account    sufficient      pam_permit.so

auth       required        pam_ldap.so
account    required        pam_ldap.so
